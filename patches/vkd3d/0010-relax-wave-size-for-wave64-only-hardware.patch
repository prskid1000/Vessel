vkd3d: relax wave size validation for wave64/128-only hardware.

UE5 titles emit compute shaders with `[WaveSize(32)]` metadata. On desktop
GPUs (NVIDIA wave32, RADV wave32/64) this is always satisfiable. On Adreno
7xx/8xx the minimum subgroup size is 64 -- the shader's required range
`[32, 32]` does not intersect the device range `[64, 64]`, so
`d3d12_device_validate_shader_meta()` rejects every such PSO, the engine
sees `VK_ERROR_DEVICE_LOST`, and the game black-screens or crashes.

The fix is a new config flag pair:

  `relax_wave_size`       -- WARN once and carry on (default ON in this build)
  `no_relax_wave_size`    -- restore upstream fail-hard behavior

Three code sites change:

1. `config_flag_decl.h` / `config_flags.h`: declare the two flags, shrink
   `reserved0` from 26 to 24 bits (static assert still passes at 12 bytes).

2. `device.c` `vkd3d_instance_deduce_config_flags_from_environment()`: if the
   user has not set `no_relax_wave_size`, enable `relax_wave_size` by default.

3. `device.c` `d3d12_device_validate_shader_meta()`: when the WaveSize range
   does not intersect and `relax_wave_size` is set, WARN once instead of ERR +
   return false.

4. `state.c` `vkd3d_setup_shader_stage()`: after computing
   `subgroup_size_alignment`, clamp it into `[minSubgroupSize,
   maxSubgroupSize]` when `relax_wave_size` is set, so Vulkan never receives
   an illegal `requiredSubgroupSize`.

Shaders that truly depend on exactly 32 lanes may mis-render, but every UE5
title tested so far (Honeycomb / The World Beyond, Fortnite, ...) runs
correctly because their wave-size-sensitive paths all guard with
`WaveGetLaneCount()` checks.

Patch sourced from The412Banner/Nightlies vkd3d-proton fork.

diff --git a/README.md b/README.md
index b60a643..fac8426 100644
--- a/README.md
+++ b/README.md
@@ -208,6 +208,10 @@ commas or semicolons.
       so it should not be a real issue even on lower VRAM cards.
     - `force_host_cached` - Forces all host visible allocations to be CACHED, which greatly accelerates captures.
     - `no_invariant_position` - Avoids workarounds for invariant position. The workaround is enabled by default.
+    - `relax_wave_size` - When a shader's required `[WaveSize]` range cannot be satisfied by the device
+      (e.g. `WaveSize(32)` on wave64/128-only hardware), clamp to the nearest supported subgroup size
+      instead of rejecting the pipeline. Shaders that assume an exact lane count may mis-render.
+      **Enabled by default in this build**; `no_relax_wave_size` restores upstream behavior.
  - `VKD3D_DEBUG` - controls the debug level for log messages produced by
    vkd3d-proton. Accepts the following values: none, err, info, fixme, warn, trace.
  - `VKD3D_SHADER_DEBUG` - controls the debug level for log messages produced by
diff --git a/include/private/config_flag_decl.h b/include/private/config_flag_decl.h
index 338ef7a..267ed19 100644
--- a/include/private/config_flag_decl.h
+++ b/include/private/config_flag_decl.h
@@ -70,3 +70,5 @@ VKD3D_DECL_CONFIG("rtas_allow_blas_rebuild_sizes", RTAS_ALLOW_BLAS_REBUILD_SIZES
 VKD3D_DECL_CONFIG_PLAIN(REQUIRE_INPUT_ATTACHMENTS)
 VKD3D_DECL_CONFIG("disallow_committed_texture_suballocation", DISALLOW_COMMITTED_TEXTURE_SUBALLOCATION)
 VKD3D_DECL_CONFIG("allow_image_heap_suballocation", ALLOW_IMAGE_HEAP_SUBALLOCATION)
+VKD3D_DECL_CONFIG("relax_wave_size", RELAX_WAVE_SIZE)
+VKD3D_DECL_CONFIG("no_relax_wave_size", NO_RELAX_WAVE_SIZE)
diff --git a/include/private/config_flags.h b/include/private/config_flags.h
index 1a6a462..60ce844 100644
--- a/include/private/config_flags.h
+++ b/include/private/config_flags.h
@@ -31,7 +31,7 @@ struct vkd3d_config_flags_bitfield
 	 * we may end up with stray uninitialized bits which can subtly break bitwise operations later.
 	 * Adding more configs will cause the static assert below to fail,
 	 * which indicates the need to subtract a reserved bit. */
-	uint32_t reserved0 : 26;
+	uint32_t reserved0 : 24;
 };
 
 STATIC_ASSERT(sizeof(struct vkd3d_config_flags_bitfield) == 12);
diff --git a/libs/vkd3d/device.c b/libs/vkd3d/device.c
index c227db7..08db66c 100644
--- a/libs/vkd3d/device.c
+++ b/libs/vkd3d/device.c
@@ -1309,6 +1309,12 @@ static void vkd3d_instance_deduce_config_flags_from_environment(void)
                     .PIPELINE_LIBRARY_NO_SERIALIZE_SPIRV = 1,
                     .PIPELINE_LIBRARY_IGNORE_SPIRV = 1));
     }
+
+    /* relax_wave_size is enabled by default in this build. Mobile GPUs (e.g. Adreno 7xx, wave64/128 only)
+     * cannot honor [WaveSize(32)] and SM6-only titles (UE5) would otherwise fail every such compute PSO.
+     * Opt out with VKD3D_CONFIG=no_relax_wave_size. */
+    if (!VKD3D_CONFIG_FLAG_IS_SET(NO_RELAX_WAVE_SIZE))
+        vkd3d_config_flag_global_add(VKD3D_CONFIG_FLAG(RELAX_WAVE_SIZE));
 }
 
 static void vkd3d_instance_apply_global_shader_quirks(void)
@@ -11789,11 +11795,31 @@ bool d3d12_device_validate_shader_meta(struct d3d12_device *device, const struct
         if (meta->cs_wave_size_min > info->vulkan_1_3_properties.maxSubgroupSize ||
                 meta->cs_wave_size_max < info->vulkan_1_3_properties.minSubgroupSize)
         {
-            ERR("Required WaveSize range [%u, %u], but supported range is [%u, %u].\n",
-                    meta->cs_wave_size_min, meta->cs_wave_size_max,
-                    info->vulkan_1_3_properties.minSubgroupSize,
-                    info->vulkan_1_3_properties.maxSubgroupSize);
-            return false;
+            if (VKD3D_CONFIG_FLAG_IS_SET(RELAX_WAVE_SIZE))
+            {
+                /* The device cannot provide any wave size in the shader's required range.
+                 * Rather than failing PSO creation (which most engines treat as fatal), clamp to the
+                 * nearest supported size in vkd3d_setup_shader_stage() and carry on.
+                 * Shaders that assume an exact lane count may mis-render. */
+                static bool warned_once;
+                if (!warned_once)
+                {
+                    warned_once = true;
+                    WARN("Required WaveSize range [%u, %u] is outside supported range [%u, %u]; "
+                            "relax_wave_size is set, clamping to nearest supported subgroup size (shader %016"PRIx64", reported once).\n",
+                            meta->cs_wave_size_min, meta->cs_wave_size_max,
+                            info->vulkan_1_3_properties.minSubgroupSize,
+                            info->vulkan_1_3_properties.maxSubgroupSize, meta->hash);
+                }
+            }
+            else
+            {
+                ERR("Required WaveSize range [%u, %u], but supported range is [%u, %u].\n",
+                        meta->cs_wave_size_min, meta->cs_wave_size_max,
+                        info->vulkan_1_3_properties.minSubgroupSize,
+                        info->vulkan_1_3_properties.maxSubgroupSize);
+                return false;
+            }
         }
     }
 
diff --git a/libs/vkd3d/state.c b/libs/vkd3d/state.c
index 6dde614..3acc388 100644
--- a/libs/vkd3d/state.c
+++ b/libs/vkd3d/state.c
@@ -3231,6 +3231,17 @@ static HRESULT vkd3d_setup_shader_stage(struct d3d12_pipeline_state *state, stru
                             device->device_info.vulkan_1_3_properties.maxSubgroupSize);
                 }
 
+                /* relax_wave_size: the required range may not intersect the device range at all
+                 * (validation let it through). Clamp into [minSubgroupSize, maxSubgroupSize] so the
+                 * requiredSubgroupSize we hand to Vulkan is always legal (e.g. WaveSize(32) -> 64 on Adreno). */
+                if (VKD3D_CONFIG_FLAG_IS_SET(RELAX_WAVE_SIZE))
+                {
+                    subgroup_size_alignment = max(subgroup_size_alignment,
+                            device->device_info.vulkan_1_3_properties.minSubgroupSize);
+                    subgroup_size_alignment = min(subgroup_size_alignment,
+                            device->device_info.vulkan_1_3_properties.maxSubgroupSize);
+                }
+
                 override_subgroup_size = true;
             }
             else if (VKD3D_CONFIG_FLAG_IS_SET(FORCE_MINIMUM_SUBGROUP_SIZE) &&
